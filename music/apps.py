from django.apps import AppConfig


class MusicConfig(AppConfig):
    """
    Class that is defining our Music app (now we must add it to the installed apps in the settings.py file)
    """
    name = 'music'
